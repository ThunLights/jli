datasource db {
    provider = "sqlite"
    url      = "file:./db/database.sqlite3"
}

generator client {
    provider = "prisma-client-js"
}

model Site {
    id   String @id
    link String @unique
}
